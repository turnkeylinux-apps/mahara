#!/bin/bash -ex

dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -O $PROXY $1; cd -
}

VERSION="17.04"
URL="https://launchpad.net/mahara/$VERSION/$VERSION.2/+download/mahara-$VERSION.2.tar.gz"

dl $URL /usr/local/src

